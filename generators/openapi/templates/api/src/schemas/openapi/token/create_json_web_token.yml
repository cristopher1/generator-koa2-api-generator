components:
  schemas:
    CreateJSONWebToken:
      description: Create a JSON Web Token
      type: object
      properties:
        email:
          type: string
          format: email
          example: usertest@user.com
        password:
          type: string
          example: superuser1234567
      additionalProperties: false
