components:
  schemas:
    UpdateUser:
      description: Update user data
      type: object
      properties:
        password:
          type: string
          example: new password
        names:
          type: string
          example: new names
        surnames:
          type: string
          example: new surnames
      additionalProperties: false
